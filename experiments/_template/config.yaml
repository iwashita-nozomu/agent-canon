# @dependency-start
# responsibility Stores template experiment topic configuration.
# upstream design ../../documents/experiment-registry.md defines managed experiment expectations.
# downstream implementation run.py consumes managed run configuration.
# @dependency-end

# Defines the experiment case set or sweep space.
cases: {}

# Defines the evaluation metric and comparison direction.
metric: {}

# Defines execution-only controls such as throttling or debug behavior.
runtime: {}
