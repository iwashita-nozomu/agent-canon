name: Issue 743 repository snapshot

# Temporary transport; removed from the final implementation diff.
# Push triggering is disabled while payload chunks are staged.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-staging-marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Issue #743 payload staging only"
