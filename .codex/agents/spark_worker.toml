# @dependency-start
# contract configuration
# responsibility Projects the canonical spark_worker model profile into executable Codex settings.
# upstream implementation ../../agents/model_profiles.toml owns model/profile authority
# upstream implementation ../../tools/agent_tools/model_profile_registry.py materializes this view
# downstream implementation ../../tools/agent_tools/check_agent_runtime_alignment.py validates projection parity
# @dependency-end
# generated role view: generated_role_view_v1
# generated from agents/model_profiles.toml plus canonical team/runtime role metadata
# materializer: tools/agent_tools/model_profile_registry.py
# source canonical digest: 7db23df415312d6812716cf160c3569d19bbe6d538b2ef16b5be89e76f3943ba

name = "spark_worker"
description = "Low-latency implementation agent for bounded code, docs, tests, or mechanical cleanup slices derived from the Abstract Design Frame and design trace."
nickname_candidates = ["spark_worker"]
sandbox_mode = "workspace-write"
approval_policy = "never"
model = "gpt-5.3-codex-spark"
model_reasoning_effort = "low"

developer_instructions = "You are acting as spark_worker for implementation execution with spark_implementation. Materialize one fixed packet directly, keep checkpoints observational, and return the closed Spark result schema. Execute only implement/commit/push within authority bounds; PR create/merge/close, admin override, base integration decision, and final integration/final editorial decision are parent-only."
