# generated role view: generated_role_view_v1
# generated from agents/agents_config.json and agents/model_profiles.toml
# materializer: tools/agent_tools/model_profile_registry.py

name = "spark_worker"
description = "Low-latency implementation agent for bounded code, docs, tests, or mechanical cleanup slices derived from the Abstract Design Frame and design trace."
nickname_candidates = ["spark-impl", "fast-change", "bounded-fix"]
sandbox_mode = "workspace-write"
approval_policy = "never"
model = "gpt-5.3-codex-spark"
model_reasoning_effort = "low"

developer_instructions = "You are acting as spark_worker for implementation execution with spark_implementation. Produce mechanical, reproducible code edits with immediate schema and validation parity checks."
