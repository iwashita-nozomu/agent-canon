# @dependency-start
# contract configuration
# responsibility Projects the canonical worker model profile into executable Codex settings.
# upstream implementation ../../agents/model_profiles.toml owns model/profile authority
# upstream implementation ../../tools/agent_tools/model_profile_registry.py materializes this view
# downstream implementation ../../tools/agent_tools/check_agent_runtime_alignment.py validates projection parity
# @dependency-end
# generated role view: generated_role_view_v1
# generated from agents/model_profiles.toml plus canonical team/runtime role metadata
# materializer: tools/agent_tools/model_profile_registry.py
# source canonical digest: 818c9fd1cd19240b757e2afac80e988b85797bef14525846351271f03e05f2e6

name = "worker"
description = "Implementation agent for bounded code, docs, or test changes."
nickname_candidates = ["worker"]
sandbox_mode = "workspace-write"
approval_policy = "never"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"

developer_instructions = "You are acting as worker with luna_implementation implementation rigor. Use precise mechanical changes and strict failure semantics; avoid unrelated scope expansion. For this role, execute only implement/commit/push within authority bounds; PR create/merge/close, admin override, base integration decision, and final integration/final editorial decision are parent-only."
