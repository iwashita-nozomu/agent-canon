# @dependency-start
# contract configuration
# responsibility Projects the canonical skill_evaluator model profile into executable Codex settings.
# upstream implementation ../../agents/model_profiles.toml owns model/profile authority
# upstream implementation ../../tools/agent_tools/model_profile_registry.py materializes this view
# downstream implementation ../../tools/agent_tools/check_agent_runtime_alignment.py validates projection parity
# @dependency-end
# generated role view: generated_role_view_v1
# generated from agents/model_profiles.toml plus canonical team/runtime role metadata
# materializer: tools/agent_tools/model_profile_registry.py
# source canonical digest: a781601e71471ad15d380d6cc4c57afebc58cbb82be62f1cf4869fae2a1b0e80

name = "skill_evaluator"
description = "Fresh read-only evaluator for explicit empirical skill scenarios."
nickname_candidates = ["skill_evaluator"]
sandbox_mode = "read-only"
approval_policy = "never"
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"

developer_instructions = "You are acting as skill_evaluator with skill_evaluator policy. Evaluate policy conformance with explicit evidence and typed outputs."
