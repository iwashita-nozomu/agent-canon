{
  "version": 1,
  "title": "Runtime Profiles And Check Matrix",
  "summary": [
    "AgentCanon ships broad shared surfaces, but not every surface is mandatory for",
    "every repository task. Treat root views and tools as installed capability, then",
    "activate only the profile required by the current change.",
    "Each profile ID and strict_dependency_graph_required value is canonical input",
    "for parent AgentCanon PR graph selection; unknown IDs fail selection."
  ],
  "profile_classes": [
    {
      "id": "bootstrap",
      "profile": "Bootstrap",
      "strict_dependency_graph_required": false,
      "activates": [
        "`bootstrap.sh`",
        "`bootstrap/`",
        "lifecycle parser and state"
      ],
      "required_when": "AgentCanon installation, start/stop, target, or lifecycle state changes"
    },
    {
      "id": "tool-runtime",
      "profile": "Tool runtime",
      "strict_dependency_graph_required": false,
      "activates": [
        "Python/Rust tool dispatch",
        "catalog",
        "LSP"
      ],
      "required_when": "Shared AgentCanon analysis tools or language servers change"
    },
    {
      "id": "container",
      "profile": "Container",
      "strict_dependency_graph_required": false,
      "activates": [
        "Dockerfile",
        "entrypoint",
        "manifest limits"
      ],
      "required_when": "The shared AgentCanon tool image or container contract changes"
    },
    {
      "id": "mount-generation",
      "profile": "Mount generation",
      "strict_dependency_graph_required": false,
      "activates": [
        "target registry",
        "generations",
        "rollback"
      ],
      "required_when": "Target admission, mount generation, rollback, or concurrent lifecycle changes"
    },
    {
      "id": "codex-surfaces",
      "profile": "Codex surfaces",
      "strict_dependency_graph_required": false,
      "activates": [
        "isolated skills",
        "agents",
        "hooks",
        "runtime-local `CODEX_HOME`"
      ],
      "required_when": "Codex preparation, isolated runtime configuration, or owned-link cleanup changes"
    },
    {
      "id": "eval-archive",
      "profile": "Eval archive",
      "strict_dependency_graph_required": false,
      "activates": [
        "eval producer",
        "external spool",
        "archive Git adapter"
      ],
      "required_when": "Eval collection, source-unchanged evidence, or agent-canon-log publication changes"
    },
    {
      "id": "docs",
      "profile": "Docs",
      "strict_dependency_graph_required": false,
      "activates": [
        "README",
        "guides",
        "runtime contracts"
      ],
      "required_when": "User-facing AgentCanon command, owner, migration, or runtime documentation changes"
    },
    {
      "id": "source",
      "profile": "Source",
      "strict_dependency_graph_required": true,
      "activates": [
        "policy",
        "workflow",
        "skills",
        "canonical tools"
      ],
      "required_when": "AgentCanon source policy, workflow, skill, hook, or canonical tool changes"
    },
    {
      "id": "project",
      "profile": "Project",
      "strict_dependency_graph_required": false,
      "activates": [
        "project Docker",
        "project test runner",
        "project GPU"
      ],
      "required_when": "A parent project execution environment changes; never for AgentCanon internal tests"
    }
  ],
  "compatibility_note": [
    "Profiles may be combined, but every changed path must have one owner and one",
    "primary check route. Do not select `project` merely because a tool was invoked",
    "against a project target."
  ],
  "risk_classes": [
    {
      "risk": "boundary",
      "examples": "new mount, path default, write location, credential route",
      "required_validation": "source before/after fingerprint, resolved paths, negative escape test"
    },
    {
      "risk": "compatibility",
      "examples": "CLI rename, catalog entry, execution-plane change",
      "required_validation": "schema-v2 parity fixture for argv/cwd/streams/exit/signal/writes"
    },
    {
      "risk": "lifecycle",
      "examples": "install/start/stop/gc/uninstall, image/container ownership",
      "required_validation": "exact IDs, labels, limits, health and absence readback"
    },
    {
      "risk": "concurrency",
      "examples": "target add, rollback, task admission",
      "required_validation": "lock trace, active count, candidate/old generation evidence"
    },
    {
      "risk": "archive",
      "examples": "collect/sync, branch, push, readback",
      "required_validation": "local spool, remote ref/tree/blob digest, duplicate/conflict result"
    },
    {
      "risk": "docs",
      "examples": "user route, owner, migration, issue reference",
      "required_validation": "link/header check and clean command examples"
    }
  ],
  "risk_note": [
    "If a focused command is unavailable in a fresh checkout, report the missing",
    "owner/route; do not create a fallback that writes into source. Use one task",
    "temporary directory outside the source checkout and one shared tool image at",
    "most. Track any image/container created for validation and remove that exact",
    "resource after evidence is captured. Creator-owned temporary resources also",
    "require a cleanup receipt naming each exact path or resource ID and an absence",
    "readback before `task_close.py` can report a terminal result."
  ],
  "validation_failure_response": {
    "rule": [
      "After any validation test/check failure, do not simplify, revert, delete intended behavior/tests, weaken the oracle, or downscope required validation just to pass.",
      "First record the five machine fields: `failing_contract`, `observation_level`, `cause_classification`, `intent_preservation`, and `evidence`.",
      "This runtime-profile inventory JSON is the canonical validation-failure-response taxonomy owner. `documents/runtime/runtime-profiles-and-check-matrix.md` is the generated reader projection, while `agents/canonical/CODEX_WORKFLOW.md`, `agents/canonical/CODEX_SUBAGENTS.md`, `agents/TASK_WORKFLOWS.md`, and `documents/conventions/REVIEW_PROCESS.md` are workflow, handoff, reader-map, or checklist projections that must cite this inventory instead of defining separate slug lists.",
      "Repair with approved intent preserved or escalate before intent change."
    ],
    "required_fields": [
      "failing_contract",
      "observation_level",
      "cause_classification",
      "intent_preservation",
      "evidence"
    ],
    "cause_classes": [
      "implementation_bug",
      "test_oracle_spec_mismatch",
      "fixture_environment_issue",
      "stale_generated_artifact",
      "pre_existing_unrelated_failure",
      "approved_design_user_request_conflict"
    ],
    "intent_preservation": [
      "repair_same_intent",
      "redesign_same_intent",
      "escalate_design_conflict"
    ],
    "repair_routes": [
      "repair_same_intent: repair the owning code, config, docs, workflow, fixture, environment, generated artifact, test oracle, or residual evidence route while preserving approved intent",
      "redesign_same_intent: return to design/test planning while preserving the same approved intent",
      "escalate_design_conflict: escalate approved-design/user-request conflict before any intent change"
    ]
  },
  "check_matrix": [
    {
      "changed_surface": "Markdown docs only",
      "required_check": [
        "`tools/bin/agent-canon docs check`",
        "changed-file dependency header checks"
      ]
    },
    {
      "changed_surface": "Python code/tests",
      "required_check": [
        "targeted `pytest`",
        "`python3 -m pyright`",
        "`python3 -m ruff check ...`"
      ]
    },
    {
      "changed_surface": "AgentCanon docs/workflows/skills/tools/hooks",
      "required_check": [
        "`make agent-canon-pr-check`",
        "shared-surface sync",
        "workflow/PR checks",
        "strict dependency review as the dependency-header/graph judgment owner",
        "standalone-source tool_drift coverage once",
        "docs check",
        "generated-artifact guard",
        "standalone-source prompt/accumulated evals remain in the existing static-gates owner",
        "derived shared gates exclude AgentCanon development prompt/accumulated eval producers and parent-owned diagnostics",
        "standalone shared gates remain the existing static-gates owner and add no repository-wide project-quality job",
        "derived parent workflows expose the canonical project-quality owner marker and `make ci` command; job names are not an authority",
        "no repository-wide project-quality runner is added to the shared gate"
      ]
    },
    {
      "changed_surface": "Root bootstrap or runtime lifecycle",
      "required_check": [
        "`install -> start -> target add -> status -> codex prepare -> tool/exec`",
        "`eval collect -> eval sync` or an explicit pending receipt",
        "`stop -> gc -> uninstall -> resource absence readback`"
      ]
    },
    {
      "changed_surface": "Python/Rust tool runtime or LSP",
      "required_check": [
        "targeted tool-dispatch and runtime-artifact tests",
        "schema-v2 parity evidence for argv/cwd/streams/exit/signal/writes; creator-owned temporary-path cleanup receipt and absence readback"
      ]
    },
    {
      "changed_surface": "Docker image or shared container",
      "required_check": [
        "container contract tests",
        "one labeled shared image build when Docker is available",
        "non-root, read-only source, bounded resource and LSP readback"
      ]
    },
    {
      "changed_surface": "Target mount or generation",
      "required_check": [
        "target registry and generation tests",
        "lock, active-task, candidate-health, rollback and atomic-switch evidence"
      ]
    },
    {
      "changed_surface": "Codex isolated surfaces",
      "required_check": [
        "bootstrap Codex tests and manifest readback",
        "runtime-local `CODEX_HOME` isolation",
        "collision and foreign-link negative cases"
      ]
    },
    {
      "changed_surface": "Eval collection or archive publication",
      "required_check": [
        "runtime artifact/archive tests",
        "local bare-remote E2E",
        "source unchanged, pending spool, branch and remote readback evidence"
      ]
    },
    {
      "changed_surface": "Project execution environment",
      "required_check": [
        "project-owned Dockerfile and `test/testrunner.sh`",
        "project test/GPU evidence; never AgentCanon internal test knowledge"
      ]
    }
  ],
  "closeout_rule": [
    "Do not turn optional profiles into hidden mandatory work. If a profile is not",
    "activated by the changed paths, user request, or risk class, record it as",
    "`not_applicable` instead of running or explaining its checks."
  ]
}
