{
  "version": 1,
  "title": "Runtime Profiles And Check Matrix",
  "summary": [
    "AgentCanon ships broad shared surfaces, but not every surface is mandatory for",
    "every repository task. Treat root views and tools as installed capability, then",
    "activate only the profile required by the current change.",
    "Each profile ID and strict_dependency_graph_required value is canonical input",
    "for parent AgentCanon PR graph selection; unknown IDs fail selection."
  ],
  "profile_classes": [
    {
      "id": "base-project",
      "profile": "Base project",
      "strict_dependency_graph_required": false,
      "activates": [
        "`README.md`",
        "`QUICK_START.md`",
        "`documents/README.md`",
        "project code and tests"
      ],
      "required_when": "Every template or derived repo"
    },
    {
      "id": "agent-runtime",
      "profile": "Agent runtime",
      "strict_dependency_graph_required": false,
      "activates": [
        "`AGENTS.md`",
        "`agents/`",
        "`.agents/`",
        "`.codex/`",
        "shared `tools/`"
      ],
      "required_when": "An agent performs or reviews repo work"
    },
    {
      "id": "devcontainer",
      "profile": "Devcontainer",
      "strict_dependency_graph_required": false,
      "activates": [
        "`.devcontainer/`",
        "shared post-create helpers"
      ],
      "required_when": "VS Code devcontainer or agent ergonomics are used"
    },
    {
      "id": "docker-runtime",
      "profile": "Docker runtime",
      "strict_dependency_graph_required": false,
      "activates": [
        "root `docker/`",
        "runtime packs"
      ],
      "required_when": "Dockerfile, image, pack, Jupyter, or container setup changes"
    },
    {
      "id": "github-automation",
      "profile": "GitHub automation",
      "strict_dependency_graph_required": false,
      "activates": [
        "`.github/`",
        "PR templates",
        "Actions helpers"
      ],
      "required_when": "GitHub Actions, PR automation, or GitHub path-constrained copies change"
    },
    {
      "id": "experiment",
      "profile": "Experiment",
      "strict_dependency_graph_required": false,
      "activates": [
        "`experiments/`",
        "experiment registry",
        "managed runner tools"
      ],
      "required_when": "Experiment topics, formal runs, result summaries, or research workflows change"
    },
    {
      "id": "cpp",
      "profile": "C++",
      "strict_dependency_graph_required": false,
      "activates": [
        "parent root remains language-neutral",
        "`cpp/CMakeLists.txt` as the single native project entry",
        "`cpp/cmake/`",
        "`cpp/src/`",
        "`cpp/include/`",
        "`cpp/tests/`",
        "`cpp/experiments/`",
        "C++ OOP checks",
        "`cmake -S \"$ROOT/cpp\" -B \"$ROOT/build/cpp/<profile>\" -DCMAKE_INSTALL_PREFIX=\"$ROOT/.state/cpp-install/<profile>\"`"
      ],
      "required_when": "C or C++ code, build layout, or native artifacts change"
    },
    {
      "id": "memory-and-learning",
      "profile": "Memory and learning",
      "strict_dependency_graph_required": false,
      "activates": [
        "`memory/`",
        "notes promotion",
        "learning workflows"
      ],
      "required_when": "User asks to persist memory, feedback/retrospective is observed, or agent-learning is in scope"
    },
    {
      "id": "maintenance",
      "profile": "Maintenance",
      "strict_dependency_graph_required": true,
      "activates": [
        "inventories",
        "review backlog scan",
        "improvement guide",
        "catalog drift tools"
      ],
      "required_when": "AgentCanon maintenance, repo-wide audit, or scheduled cleanup work"
    }
  ],
  "compatibility_note": [
    "Compatibility surfaces such as legacy subtree routes may remain documented, but",
    "only under the matching compatibility profile. They are not the default path for",
    "GitHub/submodule-first repositories."
  ],
  "risk_classes": [
    {
      "risk": "Routine docs",
      "examples": "Link/text edits in project-local docs with no source contract change",
      "required_validation": "docs check for touched docs and changed-file dependency header checks"
    },
    {
      "risk": "Focused code",
      "examples": "Narrow Python or shell edit with local tests",
      "required_validation": "changed-file dependency checks, targeted tests, ruff/pyright when Python changes"
    },
    {
      "risk": "Profile change",
      "examples": "Docker, GitHub, experiment, C++, devcontainer, MCP, or memory surface change",
      "required_validation": "profile-specific checker plus targeted tests"
    },
    {
      "risk": "Shared canon",
      "examples": "`vendor/agent-canon/`, root shared views, skills, workflows, hooks, tools",
      "required_validation": "`make agent-canon-pr-check` or equivalent AgentCanon PR gate"
    },
    {
      "risk": "Large delivery",
      "examples": "repo-wide rewrite, workflow redesign, broad policy change, or user-requested comprehensive run",
      "required_validation": "run bundle, dependency review, focused and full validation gates, independent review"
    }
  ],
  "risk_note": [
    "Static analysis and reading evidence are the primary validation evidence for",
    "AgentCanon tasks. `make ci` remains the full local confidence gate, but it is",
    "not the default or only acceptable evidence for every owner-bounded change. Use",
    "operation checks, smoke runs, full CI, long test suites, benchmarks, experiments,",
    "and other broad execution as supplemental evidence when runtime behavior,",
    "integration risk, or unresolved static/read findings require them. The selected",
    "validation must match the changed paths, owner surface, and risk class, and the",
    "PR or run bundle must state why that set is sufficient.",
    "Prompt-only or prose-only edits use the surface-specific docs, prompt, eval,",
    "and dependency checks selected by the active profile; they do not automatically",
    "escalate to full `make ci`.",
    "Pydocstyle is an explicit Docstring review route, not a shared correctness",
    "requirement. The shared gate does not invoke or block on pydocstyle; active",
    "profile validation owners remain authoritative for their selected checks.",
    "An unavailable pydocstyle tool or its diagnostics fail only the explicit",
    "review command."
  ],
  "validation_failure_response": {
    "rule": [
      "After any validation test/check failure, do not simplify, revert, delete intended behavior/tests, weaken the oracle, or downscope required validation just to pass.",
      "First record the five machine fields: `failing_contract`, `observation_level`, `cause_classification`, `intent_preservation`, and `evidence`.",
      "This runtime-profile inventory JSON is the canonical validation-failure-response taxonomy owner. `documents/runtime/runtime-profiles-and-check-matrix.md` is the generated reader projection, while `agents/canonical/CODEX_WORKFLOW.md`, `agents/canonical/CODEX_SUBAGENTS.md`, `agents/TASK_WORKFLOWS.md`, and `documents/conventions/REVIEW_PROCESS.md` are workflow, handoff, reader-map, or checklist projections that must cite this inventory instead of defining separate slug lists.",
      "Repair with approved intent preserved or escalate before intent change."
    ],
    "required_fields": [
      "failing_contract",
      "observation_level",
      "cause_classification",
      "intent_preservation",
      "evidence"
    ],
    "cause_classes": [
      "implementation_bug",
      "test_oracle_spec_mismatch",
      "fixture_environment_issue",
      "stale_generated_artifact",
      "pre_existing_unrelated_failure",
      "approved_design_user_request_conflict"
    ],
    "intent_preservation": [
      "repair_same_intent",
      "redesign_same_intent",
      "escalate_design_conflict"
    ],
    "repair_routes": [
      "repair_same_intent: repair the owning code, config, docs, workflow, fixture, environment, generated artifact, test oracle, or residual evidence route while preserving approved intent",
      "redesign_same_intent: return to design/test planning while preserving the same approved intent",
      "escalate_design_conflict: escalate approved-design/user-request conflict before any intent change"
    ]
  },
  "check_matrix": [
    {
      "changed_surface": "Markdown docs only",
      "required_check": [
        "`tools/bin/agent-canon docs check`",
        "changed-file dependency header checks"
      ]
    },
    {
      "changed_surface": "Python code/tests",
      "required_check": [
        "targeted `pytest`",
        "`python3 -m pyright`",
        "`python3 -m ruff check ...`"
      ]
    },
    {
      "changed_surface": "AgentCanon docs/workflows/skills/tools/hooks",
      "required_check": [
        "`make agent-canon-pr-check`",
        "shared-surface sync",
        "workflow/PR checks",
        "strict dependency review as the dependency-header/graph judgment owner",
        "standalone-source tool_drift coverage once",
        "docs check",
        "generated-artifact guard",
        "standalone shared gates remain the existing static-gates owner and add no repository-wide project-quality job",
        "derived parent workflows expose the canonical project-quality owner marker and `make ci` command; job names are not an authority",
        "no repository-wide project-quality runner is added to the shared gate"
      ]
    },
    {
      "changed_surface": "Root shared views or submodule pin",
      "required_check": [
        "`bash tools/sync_agent_canon.sh check`",
        "`git submodule status vendor/agent-canon` evidence"
      ]
    },
    {
      "changed_surface": "Docker/devcontainer/runtime pack",
      "required_check": [
        "`bash tools/docker_dependency_validator.sh`",
        "`make docker-build-check` when build behavior changes"
      ]
    },
    {
      "changed_surface": "GitHub workflow/PR",
      "required_check": [
        "`python3 tools/ci/check_github_workflows.py`",
        "relevant GitHub Actions evidence when available"
      ]
    },
    {
      "changed_surface": "Experiments",
      "required_check": [
        "`make experiment-check`",
        "managed run evidence for formal experiment changes"
      ]
    },
    {
      "changed_surface": "C/C++",
      "required_check": [
        "`cmake -S \"$ROOT/cpp\" -B \"$ROOT/build/cpp/<profile>\" -DCMAKE_INSTALL_PREFIX=\"$ROOT/.state/cpp-install/<profile>\"`",
        "`cmake --build \"$ROOT/build/cpp/<profile>\" --parallel`",
        "`ctest --test-dir \"$ROOT/build/cpp/<profile>\" --output-on-failure`",
        "`cmake --install \"$ROOT/build/cpp/<profile>\"`",
        "project-native C++ reviewer/checker evidence, including consumer-to-provider target readback"
      ]
    },
    {
      "changed_surface": "Memory/eval/hook logging",
      "required_check": [
        "append-only artifact evidence and improvement guide/eval checks when prompts or logging fields change"
      ]
    }
  ],
  "closeout_rule": [
    "Do not turn optional profiles into hidden mandatory work. If a profile is not",
    "activated by the changed paths, user request, or risk class, record it as",
    "`not_applicable` instead of running or explaining its checks."
  ]
}
